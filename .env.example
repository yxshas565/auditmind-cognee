# Anthropic / Claude API
ANTHROPIC_API_KEY=

# Cognee config (self-hosted)
COGNEE_LLM_API_KEY=
COGNEE_LLM_PROVIDER=anthropic
COGNEE_LLM_MODEL=claude-sonnet-4-6

# Add vector DB / graph DB config here once Cognee setup is confirmed
# e.g. LANCEDB_PATH=, NEO4J_URI=, etc. — depends on which backends we pick
